import CodingChallenge

// I extracted the main contents in another function due to the amount of
// linking warnings I would get whenever I built the project.
// As to my understanding, it is a Windows issue and there is not much i can do.
// (except suppress all warnings)

handleProgramExecution()
